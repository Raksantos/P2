#include <stdio.h>
#include <stdlib.h>
#include "hash.h"
#include "fila.h"

struct hash{
    int qtd, table_size;
    struct student **itens;
};

int main()
{
    QueuePrio *fp = init_queue();
    return 0;
}